# Development of a Legal RAG (Retrieval-Augmented Generation) Assistant for UK Employment Law
## Testing and Evaluation

The testing files are organised as follows:

- `eval_sets/` contains the evaluation question sets
- `evaluation/` contains the final result files and summaries
- `logs/` contains raw testing logs
